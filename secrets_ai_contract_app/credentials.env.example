# 复制本文件为 credentials.env 并填写真实值（credentials.env 已加入 .gitignore，勿提交）。
# Copy to credentials.env and fill in. Never commit credentials.env.

OPENAI_API_KEY=sk-your-openai-api-key-here

# 应用「访问验证」页登录密码；验证通过后自动使用上面的 OPENAI_API_KEY。
BACKEND_PASSWORD=your-login-password-here
